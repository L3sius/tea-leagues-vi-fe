services:
  tea-frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: https://api.tea-osrs.com
    image: tea-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:80"